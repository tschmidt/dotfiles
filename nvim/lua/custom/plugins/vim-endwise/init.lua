-- A simple plugin that helps to end certain structures automatically.
-- In Ruby, this means adding `end` after `if`, `do`, `def`, and several
-- other keywords.
-- https://github.com/tpope/vim-endwise
return {
  'tpope/vim-endwise',
}
