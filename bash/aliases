# cd
alias ..='cd ..'
alias cdc='cd Sites/core'

# ls
alias ls='ls -F'
alias l='ls -lAh'
alias ll='ls -l'
alias la='ls -A'

# git
alias gl='git pull'
alias gp='git push'
alias gd='git diff'
alias gc='git commit'
alias gca='git commit -a'
alias gcm='git commit -m'
alias gco='git checkout'
alias gb='git branch'
alias gs='git status'
alias grm="git status | grep deleted | awk '{print \$3}' | xargs git rm"

# rails
alias sc='script/console'
alias ss='script/server'
alias sg='script/generate'
alias sd='script/destroy'
alias a='autotest -rails'
alias tdlog='tail -f log/development.log'
alias tplog='tail -f log/production.log'
alias migrate='rake db:migrate db:test:clone'
alias rst='touch tmp/restart.txt'
alias model='script/generate model'
alias migration='script/generate migration'

# jruby
alias jgem='jruby -S gem'

# misc
alias e='exit'
alias reload='. ~/.bash_profile'
alias m='mate'

# changing directory to projects in ~/Sites/
function c { cd ~/Sites/$1; }
function h { cd ~/$1; }
function slanted { cd ~/Sites/slanted_pixel/$1; }
